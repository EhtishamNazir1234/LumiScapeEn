import React from "react";
import {
  LocationWidget,
  defaultLocationWidgetItems,
} from "../../../plugins/location-widget";

const Locations = () => <LocationWidget items={defaultLocationWidgetItems} />;

export default Locations;
